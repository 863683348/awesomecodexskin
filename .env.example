# Web3Forms access key — a public inbox identifier, NOT a secret.
# Get one at https://web3forms.com (enter the email that should receive submissions).
# Astro only exposes env vars prefixed with PUBLIC_ to the client bundle,
# so the submit form (which fetch()es Web3Forms from the browser) can read it.
PUBLIC_WEB3FORMS_KEY=
